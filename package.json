{
  "name": "aws-scheduled-cron",
  "version": "1.0.0",
  "description": "Example of creating a function that runs as a cron job using the serverless `schedule` event",
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@notionhq/client": "^0.2.1"
  },
  "devDependencies": {
    "serverless-plugin-typescript": "^1.1.9",
    "typescript": "^4.3.5"
  },
  "scripts": {
    "run": "sls invoke local -f cronHandler",
    "run-remote": "sls invoke -f cronHandler",
    "publish": "sls deploy"
  }
}
